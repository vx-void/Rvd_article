# hydro_find/prompts/specs.py

# === Инструкции ===
JSON_INSTRUCTION = (
    "Верни результат В СТРОГОМ ФОРМАТЕ JSON.\n"
    "Не включай пояснений, комментариев или дополнительного текста."
)

EXTRACTION_RULES = (
    "ПРАВИЛА:\n"
    "1. Извлекай ТОЛЬКО явно указанные параметры\n"
    "2. Не придумывай отсутствующие поля\n"
    "3. Используй null для пропущенных значений\n"
    "4. Соблюдай типы: boolean, integer, string"
)

PREPROCESSING_RULES = (
    "ПРАВИЛА:\n"
    "1. Сохраняй оригинальное написание компонентов\n"
    "2. Не добавляй пояснений\n"
    "3. Строго следуй формату вывода"
)

# === Спецификации компонентов ===
_FITTINGS_SPEC = """Проанализируй запрос и извлеки параметры фитинга.
ПОЛЯ:
- standard: строка (DKOL, DKOS, NPTF, BSP, JIC и т.д.)
- Dy: целое число
- thread: строка
- armature: "штуцер", "штуцер конусный", "гайка"
- angle: целое число (0, 45, 90)
- removable_nut: boolean
- unstandard_thread: boolean
- D_out: целое число (только если указано)
- usit: boolean
- s_key: строка (только если указан)
- compact: boolean
- pin: boolean
- o_ring: boolean
- long: целое число"""

_ADAPTERS_SPEC = """Проанализируй запрос и извлеки параметры адаптера.
ПОЛЯ:
- standard_1, standard_2: строки
- thread_1, thread_2: строки
- armature_1, armature_2: "штуцер", "штуцер конусный", "гайка" (только если указано)
- angle: целое число
- s_key: строка (только если указан)"""

_PLUGS_SPEC = """Проанализируй запрос и извлеки параметры заглушки.
ПОЛЯ:
- standard: строка
- thread_type: "метрическая" или "дюймовая"
- thread: строка
- armature: "штуцер" или "гайка" (только если указано)
- s_key: строка (только если указан)"""

_ADAPTER_TEE_SPEC = """Проанализируй запрос и извлеки параметры адаптера-тройника.
ПОЛЯ:
- standard_1, standard_2, standard_3: строки
- thread_1, thread_2, thread_3: строки
- armature_1, armature_2, armature_3: "гайка", "штуцер", "штуцер конусный" (только если указано)"""

_BANJO_SPEC = "Проанализируй запрос и извлеки параметры банжо."
_BANJO_BOLT_SPEC = "Проанализируй запрос и извлеки параметры банжо-болта."
_BRS_SPEC = "Проанализируй запрос и извлеки параметры BRS."
_COUPLING_SPEC = "Проанализируй запрос и извлеки параметры муфты."

# === Спецификации предобработки ===
_TEXT_SPLIT_SPEC = "Разбей строку на отдельные компоненты — по одному на строку."
_QUANTITY_SPEC = "Извлеки количество или верни 'Не указано'."
_CLASSIFICATION_SPEC = "Верни одно из: fittings, adapters, plugs, adapter-tee, banjo, banjo-bolt, brs, coupling"